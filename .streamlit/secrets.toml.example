# Copy this file to `.streamlit/secrets.toml` and paste in your real key.
# `.streamlit/secrets.toml` is gitignored and must never be committed.
#
# For the deployed app on Streamlit Community Cloud, set this same key in the
# app's Settings → Secrets section instead of using a file.
ANTHROPIC_API_KEY = "sk-ant-your-key-here"
